---
import Container from '../layout/Container.astro';
import Card from '../ui/Card.astro';
import SectionHeading from './SectionHeading.astro';

const { items = [] } = Astro.props;
---

<section class="scroll-mt-16 py-12 md:py-16" id="problemas">
  <Container class="space-y-8">
    <SectionHeading
      eyebrow="Problemas"
      title="La mayoría de las empresas no tiene un problema de software. Tiene un problema de ejecución."
      description="Procesos manuales, datos dispersos y herramientas desconectadas siguen frenando decisiones, coordinación y capacidad real de ejecución."
    />

    <div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">
      {items.map((item) => (
        <Card class="flex h-full flex-col gap-3 p-5">
          <div class="space-y-3">
            <h3 class="text-lg font-semibold text-phx-6 md:text-xl">{item.title}</h3>
            <p class="text-sm leading-6 text-phx-7 md:text-base">{item.description}</p>
          </div>
        </Card>
      ))}
    </div>
  </Container>
</section>
